# 王晰数字图书馆 — 项目上下文

> 每次会话先读本文件（&lt;150 行）。它替代「每次重新解释背景」。
> 站点：https://wx409.github.io ｜ 仓库：D:\wx409.github.io ｜ 事实源入口：`E:\wx\论文素材_王晰作传\原始材料总索引_v1.md`

## 一、项目是什么

王晰（华语流行男低音 / Bass-baritone）的 **AI 可读知识库 + 传记素材库**。

他不红、没有媒体吹、没有权威人士表扬，现成材料少。所以站的职责是四件事：
**从零挖掘素材 → 实测挖能力 → 把复杂数据翻译给普通人 → 让 AI 可引用**，最终为传记提供可验证骨架。

**价值判断的唯一尺子：可信、可查、可引用。不是"好看"。**

### 三套数据 × 三类声音素材（交叉 = 传记三条主线）

| 数据层 | 回答 | 状态 |
|---|---|---|
| 声学实测 | 他能做到什么 | 核心已建成（72 曲录音室全量 + 现场双层 + 动态范围第一批） |
| QQ音乐指数 | 他被市场怎么对待 | 1286 天数据在，**尚未与声学交叉** |
| 微博文本 | 他是什么状态、怎么表达 | 个人微博已有，少城时期待补 |

声音素材：**全链路已完成**（下载 **157 件 / 1.16 GB**｜转写 **157 篇 / 171,098 字 / 926.1 分钟**｜
11 系列覆盖 2016–2026｜并入知识库 + 站内呈现 + 主题/情感/时间线分析）
｜巡演 Talk（六巡广州已做；一巡待指定场次）｜杂志采访（散乱待建索引）

传记三条主线：**能力-市场线**（唱得好，市场看见了吗？）｜**市场-情绪线**（他如何面对被看见/不被看见）｜**情绪-能力线**（状态如何影响声音）

## 二、核心原则（不可违背）

1. **不写死数字**：动态数值由脚本计算注入；禁止手写统计数字进 HTML/JSON/llms.txt。
2. **单一事实源**：`dashboard/dashboard_data.json` 是数据出口；基线以 `compute_baseline_v1.py` 为准；
   音域以 `generate_vocal.py` 为准；`data/calibers.md` 是数字字典（引用前必查，当前 53 项口径）。
3. **完整档案页是内容源，不删内容、不删表格**。它们不占主导航位，但**仍对外可见、仍被收录、仍推送**。
4. **诚实披露**：缺失标「待补」，不确定标「待核」，**绝不伪造**。外部读数标来源与方法状态。
5. **过程稿不上线**：全站不得出现「旧口径/已作废/结论修正/瞬时最低」，版本谱系只留 JSON-LD `isBasedOn`。
6. 遇到不确定：**暂停并询问，不自行猜测**。

## 三、仓库结构

- **对外主入口 8 个公开页**：`index.html` `works.html` `live.html` `vocal.html` `history.html`
  `research.html` `community.html`（精简版 7 页）+ `qa.html`（问答库 16 条实质问答，**已按方案 A 扶正为第 8 个公开页**）
- **完整档案页 25 个**：`index,follow`、在 sitemap、推送 IndexNow，**只在底部全站索引有入口**
  （`voice/stage/skill/academic/dashboard/live-reviews/discography/songs/timeline/…`）
- **私密/实验区（noindex，不推送）**：`archive-index.html`（完整档案索引）、`kb-semantic.html`、
  `debate/`（争议案例）、`search.html`（工具页）
- `data/`：JSON 数据层（见 `docs/DATA-SCHEMA.md`）｜`tools/`：脚本 ｜ `project_b/`：生成器与审计
- `docs/`：本项目的规则与档案（见下）

## 四、声学数据铁律（三级读数制度）

| 级别 | 定义 | 可否引用 |
|---|---|---|
| **稳定音（能力口径）** | 过复核门槛：音符本身 ≥0.2s、HNR ≥5dB、强度 ≥中位−25dB，并经谐波列/CREPE 交叉校验 | ✅ 可作能力结论、可进标题与对外引用句 |
| **触达音** | 实际到过的最低 F0，未满足门槛 | ❌ 展示但不作能力依据，不进汇总、不进标题 |
| **低音带读数** | 归属未确认（人声/乐器/念诵） | ❌ 仅供参考；三不许：不进汇总、不进统计、不进对外引用句 |

- 测量管线：`demucs htdemucs 人声分离 → 自研 numpy YIN 逐帧 F0（fmin 55/fmax 1100/frame 2048/hop 512/sr 22050）
  → 音符切分（≥80ms、抖动 <0.6 半音）→ 稳健过滤`；低音读数另做**原始混音谐波列**判定。
- **A1 类读数不列、不提、不引用**（未过复核门槛）。
- **《向着太阳》最低稳定音 = G2 97.8Hz**（2026-09-09 CREPE 复核；不是 D#2）。

## 五、关键数据锚点（引用前请核对 `data/calibers.md`）

- 最低稳定音 **B1 61.5Hz**《静止了的夜晚》（72 首录音室曲目全量实测）
- 低音主区 **B1–D2（61.5–75.5Hz）**，全部低于男低音下限 E2（82.4Hz）
- 跨度中位 **2.30 个八度**｜音符内稳定性中位 **7 音分**｜颤音 **4.89–5.17Hz / 86 音分**
- B1 曲目：17 曲层 **3 首**；72 曲全量 **4 首**（两个口径不同，不可混读）
- 巡演 **59 场（全站 64 场 = 巡演 59 + 签唱会 5）/ 22 城**｜指数覆盖 **1286 天**
- 现场 vs 他人主导舞台最低稳定音**差异不显著（p=0.197）**

## 六、技术约束

- **禁用 Jekyll**（根目录有 `.nojekyll`）：不用 `_includes`、不用 `{% include %}`，复用的文本块逐页复制。
- 本机二进制受限：`numba/llvmlite.dll` 与 `sphn` 的 `.pyd` 被应用控制策略阻止
  → 不用 `librosa.pyin`、不用 `python -m demucs`；用 `sphn` 桩 + `demucs.api.Separator` + `HF_HUB_OFFLINE=1`。
  CJK 路径会让 demucs/ffmpeg 子进程失败，须先转 ASCII 名。
- **合规边界**：音视频仅用于个人研究，**不二次分发、不入库、不嵌入页面**，只发布方法与结果数据；
  敏感文件（cookie/.env/secrets/音频/qqmusic.txt）绝不入 git；公开 JSON 不得含本地绝对路径。
- 中位数一律用 `statistics.median`，禁止 `sorted(x)[len(x)//2]`。

## 七、当前阶段

| 批次 | 内容 | 状态 |
|---|---|---|
| 第一批 | 网站修补：sitemap 全量、archive-index 瘦身、llms.txt 一致性、计划外页面清单 | ✅ 完成 |
| 第二批 | **项目上下文文件：AGENTS.md + docs/**、qa.html 质量抽查 | ← 当前 |
| 第三批 | 数据采集：微博三账号、电台元数据、巡演 Talk 试点 | 待办 |
| 第四批 | 交叉分析 + 传记工程：时间轴对齐、三条主线、biography/ 目录、声学扩展 | 待办 |

## 八、文档索引（按需读，别全读）

- `docs/PROJECT.md` — 项目目标、历史决策、第一性原理
- `docs/DATA-SCHEMA.md` — `data/` 下每个 JSON 的用途与字段
- `docs/CALIBER.md` — 三级读数制度完整定义 + 引用前检查清单
- `docs/CHANGELOG.md` — 重大变更
- `docs/链接登记.md` — 全部链接（本地留存，含收录/推送状态）
- `docs/qa抽查_20260913.md` — qa.html 抽查结论（**方案 A 已执行**：16 条实质问答 + 歌曲×场次索引表）
- `docs/计划外页面清单_20260913.md` — 8 个边界页面报告
- `temp/运维备忘_20260907.md` — 逐日运维备忘（2611 行，**重大操作前查最近章节**）
- `操作中心.bat` — 150 项运维命令（GBK 编码）

## 九、遇到不确定时

**停下来问。** 尤其是：数字口径、页面该不该动、外部素材能否引用。
宁可少做一步，不要写进一个无法回溯的数字。
