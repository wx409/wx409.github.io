# 计划外页面清单（2026-09-13）

> 范围：不在「17 个完整档案页 + 7 个精简页」方案内、但有实际内容的 8 个页面。
> **本清单只报告，不处理**（除 `qa.html` 的 llms.txt 标注外，未改动任何一页）。
> 现状 robots：除 `jazz.html` 为显式 `index, follow` 外，其余均无 robots 标签（= 默认可收录）。

| # | 文件名 | 页面用途 | 是否有独立内容 | 当前状态 | 建议 |
|---|---|---|---|---|---|
| 1 | `qa.html` | 问答库，315 条问答对（真实 HTML + FAQPage JSON-LD） | 有（96.8% 为自动生成的场次问答，见 `docs/qa抽查_20260913.md`） | index, follow · 在 sitemap · 已推送 | **保留索引**，但**暂不扶正为主入口**：先按抽查报告做两段式改造（实质问答留页 + 场次问答降级为索引表） |
| 2 | `about.html` | 关于本站：维护团队、信息来源、审核原则、更新日志 | 有（llms.txt 的「更新日志」指向它） | index, follow · 在 sitemap · 已推送 | **保留索引**（站点可信度要素，且 llms.txt 引用） |
| 3 | `story.html` | 数据故事页（Effect 数据叙事） | 有（含 complete JSON-LD：Article/Dataset/FAQPage/Person） | index, follow · 在 sitemap · 已推送 | **保留索引**（是唯一把效应数字讲成故事的可引用长页） |
| 4 | `jazz.html` | 爵士专题 | 内容较薄（约 9KB，以串场/改编为主） | index, follow（显式） · 在 sitemap · 已推送 | **保留索引但在 sitemap 降权**；内容补厚前不建议进导航（现在也确实不在主导航） |
| 5 | `notifications.html` | 自动通知（站内通知流，客户端读 `data/notifications.json`） | 有（动态内容，约 38KB 数据） | index, follow · 在 sitemap · 已推送 | **保留索引**：动态页是「站点活跃度」信号，对抓取频率有利；但**不进主导航**（信息流不适合当入口） |
| 6 | `social_wall.html` | 社交动态墙：观众在微博/小红书/B站/抖音的公开反馈（匿名化） | 有（首页「社交动态墙 →」链接它） | index, follow · 在 sitemap · 已推送 | **保留索引**。备忘 2026-09-11 已纠正：它**不是僵尸页**（首页有入口），务必不要 noindex |
| 7 | `data-timeline.html` | 数据时间线（指数与事件的时间序列视图） | 有（约 21KB + 数据可视化） | index, follow · 在 sitemap · 已推送 | **保留索引**；与 `story.html` 有内容重叠，但一个是时间线、一个是叙事，可共存 |
| 8 | `story-hui-guangzhou-2026.html` | 六巡广州站单场数据复盘专题 | 有（26KB，含 MusicEvent/Article/FAQPage） | index, follow · 在 sitemap · 已推送 | **保留索引**（专项复盘是 GEO 高价值长文），并已在 `live.html` 与底部索引有直连 |

## 补充说明

- **`kb-semantic.html`**（虽不在这 8 个之列，但属同类边界）：`noindex, follow`，
  已移出 sitemap（备忘 2026-09-11 决策：旧语义入口，URL 仍可用保书签兼容）——**维持现状**。
- **`search.html`**：工具页，本次按用户指令列入 noindex（不进 sitemap、不推送），
  但仍是 `index.html` 检索表单的目标页（功能需要）。
- 上述页面**全部保留原有内容**，本次未做任何删改。

## 需要你决策的三项

1. `qa.html`：是否按 `docs/qa抽查_20260913.md` 的建议做两段式改造？（改造前不扶正）
2. `jazz.html`：是否要补厚内容，或直接并入 `research.html` 的「爵士/改编」小节？
3. `notifications.html` / `social_wall.html`：是否要保留在底部全站索引里（现状是保留）？
