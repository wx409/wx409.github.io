一键更新.bat
secrets.bat
__pycache__/
*.pyc
.env
# 深夜电台本地代理：登录 Cookie（仅存本机，绝不入库）
tavern/radio_cookie.json
# 深夜电台本地代理：含 Cookie 处理逻辑，不公开（本地保留运行）
tavern/radio_proxy.py
# 原始明细数据（若被复制到仓库则拒绝提交）
raw_archive/
*.log
debug_html/
# 本地临时目录（CSV 导出、调研中间产物、执行脚本）
temp/
# 监测中间产物（每次跑就变，非内容资产，不入库）
data/pending_web.json
data/pending_netease.json
data/netease_catalog.json
# 本地 DSH 启动脚本（含本机路径）
start-dsh.bat
# 本地一键部署脚本（含本机路径，双击运行）
一键部署推送.bat
# 本地 ACL 收紧脚本（本机专用，双击运行）
一键设置DSH目录ACL.bat
# 本地 DSH 极简模式（WSL）相关文件（含本机路径，勿公开）
启动DSH极简模式.bat
DSH极简模式-使用说明.md
DSH极简模式-场景使用指南.md
DSH双实例-完整使用说明.md
设置极简模式最强版.bat
# 本地建言草稿（不外发，避免误提交）
建言_*.md
对策建言_*.md
# 操作备份文件（避免误提交 .bak）
*.bak
*.bak_*
.bak_*
# DPAPI 加密密钥文件（绑定本机用户，拷到别处解不开，永不入库）
*.dpapi
# 本机部署操作说明（含本机路径，不入库）
部署操作说明.md

# 本机 DSH 升级操作说明（含本机路径与升级细节，不公开）
DSH升级操作说明-*.md

# DSH portable 配置/压缩包：留在本机或走私有仓库/离线，不进公开站点
dsh-config-portable/
dsh-config-portable.zip
# 本地同步方案文档（含本机路径与设想，不公开）
台式机DSH同步通道方案-20260821.md
台式机DSH安装与同步方案-20260821.md
