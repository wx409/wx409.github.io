# 统一换行符：仓库内一律 LF，checkout 时按各自平台自动转换。
# 消除 Windows 下 LF/CRLF 造成的无谓 "modified" 噪音。
* text=auto

# Windows 批处理（GBK 编码）：仓库内 LF，checkout 时 CRLF。
# 显式声明避免 GBK 编码被 text=auto 判为二进制而不做归一化，造成整文件换行符噪音。
*.bat text eol=crlf

# 二进制文件不做换行符转换，避免损坏
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.mp4 binary
*.mp3 binary
*.flac binary
*.xlsx binary
*.xls binary
*.pdf binary
*.docx binary
*.zip binary
