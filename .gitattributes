# 统一换行符：仓库内一律 LF，checkout 时按各自平台自动转换。
# 消除 Windows 下 LF/CRLF 造成的无谓 "modified" 噪音。
* text=auto

# 二进制文件不做换行符转换，避免损坏
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.mp4 binary
*.mp3 binary
*.flac binary
*.xlsx binary
*.xls binary
*.pdf binary
*.docx binary
*.zip binary
